name: http_server
description: 内嵌HTTP服务器，为应用提供本地服务
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  path: ^1.9.0
  mime: ^1.0.6


dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8