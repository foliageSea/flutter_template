output: dist/
variables:
  FLUTTER_ROOT: ~/fvm/versions/3.32.3
# PGYER_API_KEY: your api key
releases:
  - name: dev
    jobs:
      - name: windows-zip
        package:
          platform: windows
          target: zip
      - name: windows-exe
        package:
          platform: windows
          target: exe

